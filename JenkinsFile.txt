pipeline {
    agent {
        docker { image 'provider=virtualbox' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}